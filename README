# install

./install.sh

# install vim in macOS

brew install vim --with-lua --with-override-system-vi --with-python3
brew install tig git ctags

# Troubleshooting

> brew install reattach-to-user-namespace
